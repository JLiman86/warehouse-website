import { aboutData } from "../data";

const About = () => {
  const { image, title, subtitle } = aboutData;
  return (
    <section className="section">
      <div className="container mx-auto">jhh</div>
    </section>
  );
};

export default About;
